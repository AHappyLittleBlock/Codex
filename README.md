# Codex

This repository contains sample code for various machine learning tasks.

- `text_recognition/` – Example code to train a character recognition model and
  export it to Core ML for use in Swift apps on macOS or iOS.
- `image_classification/` – Train an image classifier from your own offline
  images and convert it to Core ML for iPhone or Mac apps.
- `mac_text_trainer/` – SwiftUI app for macOS 13+ to train a text classifier
  offline and test predictions.
- `text_generation/` – Script to call OpenAI or Gemini to generate text and
  remove blank lines from the result.
